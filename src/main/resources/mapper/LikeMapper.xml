<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kotu.koreatourism.mapper.LikeMapper">

    <select id="findLikeByUser" resultType="Like">
        select *
        from LIKE_TB
        where user_id = #{userId} AND content_id = #{contentId}
    </select>

    <insert id="increaseLike">
        insert into
            LIKE_TB(user_id, content_id, like_count)
        values (#{userId}, #{contentId}, #{likeCount})
    </insert>

    <delete id="decreaseLike">
        delete from LIKE_TB
        where like_id = #{likeId}
    </delete>

    <select id="findAllLikesByUser">
        select *
        from PLACESAVE_TB AS PST
        left join LIKE_TB AS LT
        on PST.content_id = LT.content_id
        where LT.user_id = #{userId}
        order by created_date desc
    </select>
</mapper>